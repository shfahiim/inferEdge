# Simple Chat App

Minimal chat UI for an OpenAI-compatible API.

## Run

```bash
npm start
```

Then open `http://localhost:3000`.

## API settings

- Base URL default: `https://poco.fahiim.me/v1`
- Endpoint used: `POST /chat/completions`
- The page stores your API key in browser local storage for convenience.
