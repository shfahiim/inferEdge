# Instructions

Use `plan.md` as the source of truth for implementing the inference-server feature.

Before making implementation changes:

- read `plan.md`
- follow the phases in `plan.md`
- keep the first implementation limited to the MVP described in `plan.md`
- reuse the existing model initialization and runtime flow where possible
- prefer incremental delivery in the order described by `plan.md`

When in doubt, align decisions with `plan.md` first.
